{
  "name": "agentip-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "contracts",
    "widget"
  ],
  "scripts": {
    "vercel-build": "npm run build --workspace=frontend"
  }
}
